{
  "functions": {
    "proxy.php": {
      "runtime": "vercel-php@0.6.0" // Vercel支持的PHP运行时（含cURL扩展）
    }
  },
  "routes": [
    { "src": "/", "dest": "/index.html" }, // 默认访问HTML
    { "src": "/proxy.php", "dest": "/proxy.php" } // 转发接口路由
  ]
}
